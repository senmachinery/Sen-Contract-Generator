# 🚑 应急手册 — Sen 网页工具出问题时怎么办

> 给 **Max** 的快速操作卡。人在外面、只有 iPad 时照这个做。
> **核心心法：先回滚止血，再慢慢修。慌没用——反正随时能退回上一个好用的版本。**

---

## 一、先判断（10 秒）

- 是不是 **刚改完就坏的**？→ 是 → 看【二、回滚】，先恢复营业。
- 不确定 / 是老毛病 → 跳到【三、用 iPad 修】。

---

## 二、回滚（最安全的止血，不用写代码）

每次部署都是一次 GitHub 提交，所以随时能退回上一个好用的版本：

1. iPad 上打开 **github.com**（或 GitHub App），进 `Sen-Contract-Generator` 仓库
2. 点 **Commits**（提交历史）
3. 找到出问题的那次改动 → 点 **Revert（撤销）**
4. 确认，生成一个"回滚提交"
5. 等约 **2 分钟**（GitHub Pages 重新发布）→ 浏览器里 **强制刷新** 网页

> 或者：把你存档的 **上一版 `index.html` / `crm.html`**（带版本号那份）整篇贴回 GitHub 网页编辑器，提交即可。

---

## 三、用 iPad 修 bug（claude.ai/code）

1. Safari 打开 **claude.ai/code**，用 **同一个账号** 登录
2. 第一次会让你 **连 GitHub** → 选仓库 `Sen-Contract-Generator`
3. **第一句先说**：「先读仓库根目录的 CLAUDE.md，严格照里面的规矩改。」
4. 再描述问题（见【四】）
5. 它会开一个 **PR（修改提案）** → 你看一眼 → **合并（Merge）**
6. 等约 2 分钟 → 强制刷新

---

## 四、描述 bug 时，告诉 Claude 这些（修得准）

- **哪个工具**：合同生成器（`index.html`）还是 CRM（`crm.html`）？
- **怎么触发**：你点了什么、做了什么操作？
- **期望 vs 实际**：本来应该怎样，结果变成怎样？
- **报错**：有红字/报错就截图或抄给它。
- **从哪一版开始坏的**（如果知道）。

---

## 五、提醒 Claude 守的红线（CLAUDE.md 里都有，这里再强调）

- ✅ **只加不改**：新功能默认关闭，绝不动原来好用的部分。
- ✅ **改了就升版本号**（标题 + 顶栏：生成器 `v10.xx`、CRM `v2.xx`）。
- ✅ **前端能修就别动后端**（动后端要重新部署，风险大）。
- ✅ 让它 **明确告诉你部署步骤**、要不要重新部署后端。
- ✅ 让它说清楚 **验证了什么**，别只说"应该没问题"。

---

## 六、改完部署后

- GitHub 上提交/合并后 → 等约 **2 分钟** Pages 重建 → **强制刷新**（清缓存）才看得到新版。

---

🧊 **记住**：最坏情况就是回滚到昨天的版本。生意不会真的停摆——深呼吸，一步步来。
